#ifndef SECRETS_H
#define SECRETS_H

/* WiFi Credentials */
#define WIFI_SSID "ESL2"
#define WIFI_PASS "rawatza007"

/* MQTT Broker IP (Local machine IP for development, RPi IP for production) */
#define MQTT_BROKER "10.42.0.1"

#endif
